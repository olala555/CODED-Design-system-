import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SponsorsPage() {
  return (
    <PlaceholderPage
      eyebrow="Sponsors"
      title="Co-branding rules and sponsor placement guidelines."
      description="Approved sponsor placement, clear-space rules, and a downloadable sponsor logo library — per CODED program."
      icon="handshake"
      next={[
        "Sponsor logo library (light + dark variants)",
        "Approved placement rules per program",
        "Co-branded template generator",
      ]}
    />
  );
}
